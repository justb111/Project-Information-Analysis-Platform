# Jira 风险分析平台 — 开发说明

---

## 一、项目分工与权限（重要！）

本项目的代码按模块严格划分归属，每位开发人员**只能修改自己负责的模块**，其余代码为项目负责人（@Justb111）所有，**严禁触碰**。

| 负责人 | 负责模块 | 可修改的文件 |
|--------|----------|-------------|
| **@Justb111** | 核心系统（风险分析、Jira数据、AI引擎、知识库、前端框架） | 除下方标注外的所有文件 |
| **陈正** | **人力洞察看板** | `workforce_api.py`（全权） |
| **邓沁怡** | **交付路线图看板** | `delivery_api.py`（全权） |

> ⚠️ **红线**：两人只能改各自的一个 .py 文件，以及前端 `index.html` / `script.js` / `style.css` 中对应自己看板标签页的那部分代码。**不可修改** `e.py`、`risk_agent.py`、`web_api.py`、`utils.py`、`excel_parser.py`、`progress_analyzer.py` 等任何其他后端文件。

---

## 二、系统概述

本项目是一个面向 **传音（Transsion）** 内部团队的 **Jira 风险分析与项目管理助手**，主要功能包括：

- 从 Jira 自动拉取 Bug / 任务数据
- 通过 AI（LLM）进行项目风险分析、质量评估、进度追踪
- 提供多个功能看板：风险看板、项目进度看板、人力洞察看板、交付路线图看板
- 知识库管理（上传文档、向量化、语义检索）
- 支持飞书文档集成
- 所有界面通过浏览器访问，支持 SSE 流式 AI 响应

---

## 三、技术栈

| 技术 | 用途 |
|------|------|
| Python 3.10+ | 后端语言 |
| Flask | Web 框架 |
| LangChain | AI Chain / Prompt / Memory 编排 |
| ChromaDB | 向量数据库（语义搜索） |
| SQLAlchemy | ORM（知识库元数据存储） |
| OpenAI 兼容 API | AI 大模型调用（公司内网代理） |
| HTML / CSS / JavaScript | 前端，原生 + Chart.js + ECharts |
| Waitress | 生产级 WSGI 服务器（支持 Windows） |
| SSE (Server-Sent Events) | AI 流式逐字输出 |

---

## 三、项目目录结构与所有权

```
E:\AI\test2\jira_risk/
│
├── e.py                      # [@Justb111] 核心入口：Flask应用 + Jira数据获取 + AI分析
├── web_api.py                # [@Justb111] LangChain版Flask应用（统一API入口）
├── run_production.py         # [@Justb111] 生产环境启动脚本
│
├── risk_agent.py             # [@Justb111] 风险分析智能体（LLM驱动）
├── utils.py                  # [@Justb111] 共享工具函数（两人可调用但不可修改）
│
├── excel_parser.py           # [@Justb111] Excel列名自动映射（两人可调用但不可修改）
├── progress_analyzer.py      # [@Justb111] 项目进度分析
│
├── jql_templates.json        # [@Justb111] JQL查询模板
├── jira_rules_knowledge.json # [@Justb111] Jira规则知识库
│
├── langchain_components.py   # [@Justb111] LangChain核心组件
├── langchain_llm.py          # [@Justb111] LLM包装器
├── langchain_tools.py        # [@Justb111] LangChain工具
│
├── delivery_api.py           # [邓沁怡] ★ 交付路线图看板 API — 全权负责
├── workforce_api.py          # [陈正]    ★ 人力洞察看板 API — 全权负责
├── knowledge_api.py          # [@Justb111] 知识库 API
│
├── knowledge_models.py       # [@Justb111] 知识库数据库模型
├── knowledge_categories.py   # [@Justb111] 知识库分类
├── chunk_processor.py        # [@Justb111] 文件智能切片
├── vector_database.py        # [@Justb111] 向量数据库（ChromaDB）
├── storage_manager.py        # [@Justb111] 文件存储管理
├── feishu_integration.py     # [@Justb111] 飞书文档集成
│
├── agents/
│   ├── base.py               # [@Justb111] BaseAgent抽象基类
│   └── risk.py               # [@Justb111] RiskAgent实现
│
├── llm/chains.py             # [@Justb111] LangChain链
├── services/ai_service.py    # [@Justb111] AI服务接口
│
├── session_manager.py        # [@Justb111] 会话管理
├── scheduler.py              # [@Justb111] 定时任务
├── strategy_manager.py       # [@Justb111] 分析策略
│
├── index.html                # [@Justb111] 主前端页面
│   ├── 人力洞察看板 Tab       # [陈正] 可修改该标签页的HTML
│   └── 交付路线图看板 Tab     # [邓沁怡] 可修改该标签页的HTML
│
├── risk_kanban.html          # [@Justb111] 遗留问题风险看板
├── script.js                 # [@Justb111] 前端主逻辑
│   ├── workforce相关函数     # [陈正] 可修改人力看板的前端逻辑
│   └── delivery相关函数      # [邓沁怡] 可修改交付看板的前端逻辑
│
├── style.css                 # [@Justb111] 前端样式
│   ├── workforce相关样式     # [陈正] 可修改人力看板样式
│   └── delivery相关样式      # [邓沁怡] 可修改交付看板样式
│
├── frontend/                 # [@Justb111] Vite前端项目（备选）
├── static/                   # [@Justb111] 静态文件
│
├── .env                      # [@Justb111] 环境变量
├── requirements.txt          # [@Justb111] Python依赖
│
├── temp_delivery/            # [邓沁怡] 交付路线图临时文件目录
├── temp_workforce/           # [陈正]    人力洞察临时文件目录
├── temp_uploads/             # [@Justb111] 上传临时文件
├── uploads/                  # [@Justb111] 上传文件存储
├── chroma_db/                # [@Justb111] ChromaDB持久化
└── knowledge.db              # [@Justb111] 知识库SQLite
```

---

## 四、各模块详细说明

### 4.1 核心入口：e.py 与 web_api.py（[@Justb111]）

**e.py** 是最原始的核心文件，包含：

- **Flask 应用初始化**：路由注册、CORS、登录认证
- **Jira 数据获取**：`fetch_all_issues(jql)` 通过 Jira REST API 拉取数据
- **AI 分析**：`stream_ai_to_queue()` 调用 AI API 流式输出分析结果
- **数据格式化**：`format_portfolio_data()` 将 Jira 数据转为 AI 可读文本，含 Must_Resolve / 标签风险标记
- **看板数据接口**：提供项目风险看板所需的后端数据 API

**web_api.py** 在 e.py 基础上引入 LangChain 架构，保持现有 API 接口不变：

- 使用 `from e import fetch_all_issues, format_portfolio_data` 复用 e.py 的数据能力
- 集成所有 Blueprint：知识库 `/api/knowledge`、人力 `/api/workforce`、交付 `/api/delivery`
- 由 `run_production.py` 启动生产服务

> **合作者注意**：这两个文件已由 @Justb111 完成，**不要修改**。你只需要确保你的 Blueprint 在 `web_api.py` 中被正确注册即可（已注册，`delivery_bp` 和 `workforce_bp` 都已导入）。

---

### 4.2 风险分析智能体：risk_agent.py（[@Justb111]）

系统 AI 大脑，用纯 LLM 驱动决策：

1. **意图识别** → LLM 判断用户意图（风险分析 / 项目概况 / Bug 查询等）
2. **JQL 生成** → LLM 根据意图生成 JQL，模板库提供 project 子句
3. **数据获取** → 调用 `e.py` 的 `fetch_all_issues()` 获取全量数据
4. **状态过滤** → 按 status 判定未解决（只有 abandoned/closed 算已解决）
5. **分析输出** → AI 分析全量数据，风险聚焦未解决，重点分析 Must_Resolve=MP Block 和 阻塞测试标签

关键设计点：

- `_detect_projects_in_query()` — 本地项目名校验
- `_get_project_jql_clause()` — 从模板提取 project 子句（已去掉 summary 和日期限制）
- `_filter_unresolved_by_status()` — 按状态机判定未解决
- `RiskAnalysisAgent.process_query()` — 主流程入口

---

### 4.3 共享工具函数：utils.py（[@Justb111]，两人只读不可改）

所有 AI 调用的统一入口：

- **`call_ai_api()`** — 封装请求头（代理鉴权）、重试机制、超时控制、工具调用
- **`parse_thinking_answer()`** — 解析 `<thinking>` 和 `<answer>` 标签
- **`process_sse_stream()`** — 解析 SSE 流式响应
- **`generate_sse_message()`** — 生成 SSE 格式消息

> **合作者**：你们需要调用 AI 分析时，直接 `from utils import call_ai_api, generate_sse_message` 使用即可，**不要自己封装 HTTP 请求**。

---

### 4.4 Excel 解析：excel_parser.py（[@Justb111]，两人只读不可改）

- 自动识别 Excel 列名（中英文别名映射）
- 输出统一的结构化字典列表

> **合作者**：你们上传 Excel 的场景可以用 `excel_parser.py` 的 `infer_columns()` 辅助识别列名，但**不要修改此文件**。

---

### 4.5 两个看板 API（合作者专属）

#### 4.5.1 人力洞察看板：workforce_api.py（[陈正] ★ 全权负责）

**路由前缀**：`/api/workforce`

**功能**：
- 上传 Excel 解析人力数据
- 支持三种数据类型：
  - **A类（人员任务表）**：姓名、总任务、完成数、所属团队
  - **B类（团队效率表）**：业务组、预估、实际、提效
  - **HR类（人力负载表）**：部门、姓名、利用率、任务数、请假等
- 提供统计分析和可视化数据

**你（陈正）需要关注的核心函数**：

| 函数 | 位置 | 说明 |
|------|------|------|
| `workforce_bp` | 第14行 | Blueprint定义，路由前缀 `/api/workforce` |
| `COLUMN_KEYWORDS_A/B/HR` | 第23-48行 | 列名关键词映射表 — 如果新Excel列名不对应可扩展这里 |
| `parse_A()` / `parse_B()` / `parse_HR()` | 各自区域 | 三种数据类型的解析逻辑 — 核心解析函数 |
| `upload_and_analyze()` | POST路由 | 上传接口，接收 Excel 调用解析 |
| `get_analysis()` | GET路由 | 返回分析结果数据 |

**可调用的共享函数**（已有，直接 import 使用）：
- `utils.call_ai_api()` — AI 分析调用
- `utils.generate_sse_message()` — SSE 推送
- `excel_parser.infer_columns()` — 列名识别辅助

**临时文件目录**：`temp_workforce/`

**⚠️ 不可修改的文件**：`e.py`、`web_api.py`、`risk_agent.py`、`utils.py`、`excel_parser.py`、`progress_analyzer.py`、`knowledge_*.py`、`delivery_api.py`（邓沁怡的）

---

#### 4.5.2 交付路线图看板：delivery_api.py（[邓沁怡] ★ 全权负责）

**路由前缀**：`/api/delivery`

**功能**：
- 上传 Excel 自动检测 Sheet 类型（9种）
- 类型包括：路线图矩阵、人力模型、风险看板、编制计划、版本计划、转维、项目统计、技术路线图、简单列表
- 聚合为组合级（Portfolio）视图
- 输出可视化数据与 AI 分析

**你（邓沁怡）需要关注的核心函数**：

| 函数 | 位置 | 说明 |
|------|------|------|
| `delivery_bp` | 第18行 | Blueprint定义，路由前缀 `/api/delivery` |
| `COLUMN_KEYWORDS` | 第26-41行 | 通用列名映射表 — 可扩展 |
| `SHEET_TYPE_SIGNATURES` | 第45-55行 | Sheet类型检测签名 — 添加新类型在这里 |
| `_detect_sheet_type()` | 自动检测 | 根据关键词判断 Sheet 类型 |
| `_parse_sheet()` | Sheet解析 | 按类型解析为结构化数据 |
| `_build_portfolio_view()` | 聚合视图 | 多Sheet聚合法合级视图 |
| `upload_excel()` | POST路由 | 上传解析入口 |
| `get_view()` | GET路由 | 返回看板数据 |
| `stream_ai_analysis()` | AI分析 | 调用 AI 分析看板数据 |

**可调用的共享函数**（已有，直接 import 使用）：
- `utils.call_ai_api()` — AI 分析调用
- `utils.generate_sse_message()` — SSE 推送
- `excel_parser` — Excel 解析辅助

**临时文件目录**：`temp_delivery/`

**⚠️ 不可修改的文件**：`e.py`、`web_api.py`、`risk_agent.py`、`utils.py`、`excel_parser.py`、`progress_analyzer.py`、`knowledge_*.py`、`workforce_api.py`（陈正的）

---

### 4.6 知识库模块（[@JustJustb111]）

| 文件 | 功能 |
|------|------|
| `knowledge_api.py` | 知识库 API：文件上传、搜索、JQL 模板索引 |
| `knowledge_models.py` | 数据库模型 |
| `knowledge_categories.py` | 文件分类管理 |
| `chunk_processor.py` | 多策略切片 |
| `vector_database.py` | ChromaDB 向量检索 |
| `storage_manager.py` | 文件存储管理 |
| `feishu_integration.py` | 飞书文档集成 |

---

### 4.7 AI / LangChain 组件（[@Justb111]）

| 文件 | 功能 |
|------|------|
| `langchain_components.py` | LangChain 核心：StreamingHandler、ContextMemory、分析链 |
| `langchain_llm.py` | 将 `call_ai_api` 包装为 LangChain LLM 接口 |
| `langchain_tools.py` | JiraQueryTool、JQLGenerator 等工具 |
| `agents/risk.py` | RiskAgent 实现（当前与 risk_agent.py 有冗余） |
| `services/ai_service.py` | AI 服务统一接口 |

---

### 4.8 前端（[@Justb111]，合作者只可改自己看板对应的部分）

**index.html** — 主界面，包含 8 个功能标签页：

| 标签页 | 归属 | 功能 |
|--------|------|------|
| 首页 | @Justb111 | 欢迎页、汇总信息 |
| 项目风险分析 | @Justb111 | AI 风险分析（SSE 流式） |
| 项目信息查询 | @Justb111 | 查询 Jira 项目信息 |
| 项目管理助手 | @Justb111 | 多轮对话式项目管理 |
| 项目进度风险看板 | @Justb111 | 上传 Excel → AI 进度分析 |
| **人力洞察看板** | **陈正** | **上传 Excel → 人力分析** |
| **交付路线图看板** | **邓沁怡** | **上传 Excel → 多类型看板** |
| 知识库管理 | @Justb111 | 文件上传、搜索、管理 |

**script.js** — 前端主逻辑，找到 `workforce` / `delivery` 相关函数进行修改。

**style.css** — 前端样式，对应看板的 CSS 部分。

> **合作者**：在前端三个文件中，**只修改你自己看板标签页对应的 HTML/JS/CSS 代码片断**。不要改动其他标签页的结构和样式。

---

## 五、启动方式

### 开发模式

```bash
python e.py --api
# 默认启动在 http://localhost:5002
```

### 生产模式

```bash
python run_production.py --host 0.0.0.0 --port 5002 --threads 100
```

---

## 六、环境变量配置（.env）

```ini
# Jira 配置
JIRA_URL=http://jira.transsion.com
JIRA_USERNAME=jira_robot
JIRA_PASSWORD=your_password

# AI 服务配置（公司内网代理）
AI_BASE_URL=https://hk-intra-paas.transsion.com/tranai-proxy/v1
AI_API_KEY=sk_your_key
AI_MODEL=gpt-5.4
X_USER_NO=your_employee_number
X_USER_NAME=your_name
X_USER_DEPT_NAME=your_department_name

# 可选
DEBUG=false
LOG_LEVEL=info
```

> 两人开发时直接用已有的 `.env` 即可，不需要额外配置。

---

## 七、API 路由一览

### 核心 API（[@Justb111]，不可动）

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | `/api/login` | 登录认证 |
| POST | `/api/chat` | AI 对话（SSE 流式） |
| POST | `/api/analyze` | 项目风险分析 |
| GET | `/api/templates` | JQL 模板列表 |
| GET | `/api/projects` | 项目列表 |

### 看板 API

| 方法 | 路由 | 归属 | 说明 |
|------|------|------|------|
| POST | `/api/workforce/upload` | **陈正** | 人力洞察上传 Excel |
| GET | `/api/workforce/analysis` | **陈正** | 人力分析结果 |
| POST | `/api/delivery/upload` | **邓沁怡** | 交付路线图上传 Excel |
| GET | `/api/delivery/view` | **邓沁怡** | 交付看板数据 |
| POST | `/api/knowledge/upload` | @Justb111 | 知识库上传 |
| GET | `/api/knowledge/search` | @Justb111 | 知识库搜索 |

---

## 八、数据流说明

```
用户输入查询
    │
    ▼
risk_agent.py (意图识别) ─── [@Justb111]
    │
    ├─ Jira 相关 → LLM 生成 JQL → e.py fetch_all_issues() → Jira REST API
    │                              │
    │                              ▼
    │                        LLM 分析全量数据 → SSE 流式返回前端
    │
    ├─ 进度分析 → progress_analyzer.py → LLM 偏差分析 ─── [@Justb111]
    │
    ├─ 人力洞察看板 → 上传 Excel → workforce_api.py 解析 → 结构化展示 ─── [陈正]
    │
    ├─ 交付路线图看板 → 上传 Excel → delivery_api.py 解析 → 结构化展示 ─── [邓沁怡]
    │
    └─ 知识库 → upload → chunk_processor → vector_database → ChromaDB ─── [@Justb111]
                              │
                              ▼
                        语义搜索 → 返回匹配片段
```

---

## 九、开发规范

1. **新增 API**：在你自己的 `workforce_api.py` 或 `delivery_api.py` 中加路由即可，Blueprint 已注册
2. **AI 调用**：统一 `from utils import call_ai_api`，不要自己封装 HTTP 请求
3. **SSE 输出**：使用 `utils.generate_sse_message(事件类型, 内容)` 
4. **文件上传**：临时文件放到 `temp_workforce/`（陈正）或 `temp_delivery/`（邓沁怡），UUID 命名
5. **日志**：使用 `print()` 或标准的 `logging` 模块，生产日志写入 `app.log`
6. **不要动别人的代码**——你只改自己负责的文件，其他文件只读引用

---

## 十、合作开发场景

### 场景 1：陈正 — 新增人力分析维度

1. 在 `workforce_api.py` 的 `COLUMN_KEYWORDS_HR` 中添加新列的关键词映射
2. 在 `parse_HR()` 中添加对应的解析逻辑
3. 在 `get_analysis()` 路由中返回新维度的数据
4. 前端 `index.html` 的 人力洞察看板 标签页展示新数据

### 场景 2：邓沁怡 — 新增交付看板类型

1. 在 `delivery_api.py` 的 `SHEET_TYPE_SIGNATURES` 中添加新类型签名
2. 在 `_parse_sheet()` 中添加新类型的解析分支
3. 在 `_build_portfolio_view()` 中集成到组合视图
4. 前端对应标签页展示

### 场景 3：两人都需要调用 AI 分析

```python
from utils import call_ai_api

response = call_ai_api(
    messages=[{"role": "user", "content": "你的分析提示词"}],
    system_prompt="你的系统提示词",
    stream=True,   # 设为 True 走 SSE 流式
    max_tokens=4096
)
```

### 场景 4：排查问题

1. 查看控制台输出的日志信息
2. 检查 `app.log` 获取详细日志
3. AI 调用问题查看 `[AI_DEBUG]` 和 `[AI_ERROR]` 标记
4. 如果是你模块的问题，只改你自己的文件

---

## 十一、代码贡献流程

1. 从 `main` 分支创建自己的功能分支（如 `feat/workforce-chenzheng`、`feat/delivery-dengqinyi`）
2. 只修改自己负责的文件
3. 提交 PR 给 @Justb111 审核
4. 审核通过后合并到 `main`
5. 生产服务器拉取最新代码，重启服务

---

如有疑问，请联系项目负责人 @Justb111
