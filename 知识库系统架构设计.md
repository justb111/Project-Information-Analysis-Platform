# 知识库系统架构设计

## 1. 项目概述
为Jira风险分析平台新增文件传输区域，支持多种文件格式，实现结构化数据库+向量数据库+对象存储的三层协同架构，解决复杂内容切片问题，让AI能够调用知识进行学习和输出。

## 2. 核心需求分析

### 2.1 文件格式支持
- **飞书云文档**（需处理权限限制）
- **本地表格**（Excel, CSV）
- **本地文档**（Word, TXT, Markdown）
- **PDF文档**
- **图片文件**（PNG, JPG等）

### 2.2 文件分类系统
1. **项目信息知识传输** - 项目文档、需求规格、设计文档等
2. **项目管理知识传输** - 项目管理方法论、流程规范、模板等
3. **Jira库规范知识传输** - Jira使用规范、工作流定义、字段规范等

### 2.3 核心挑战
1. **内容元素复杂**：表格、图表、代码块、混合格式难以精准切片
2. **快速切片困难**：需要智能的语义分割算法
3. **权限限制**：飞书云文档需要处理公司权限特性
4. **多数据库协同**：结构化+向量+对象存储的三层架构

## 3. 系统架构设计

### 3.1 三层数据库架构
```
┌─────────────────────────────────────────────────┐
│                  应用层                          │
│  • 文件上传API                                  │
│  • 知识检索API                                  │
│  • AI学习接口                                   │
└─────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────┐
│                业务逻辑层                        │
│  • 文件解析引擎                                 │
│  • 智能切片算法                                 │
│  • 向量化处理器                                 │
│  • 权限管理器                                   │
└─────────────────────────────────────────────────┘
                            ↓
┌─────────────┬─────────────┬─────────────────────┐
│ 结构化数据库 │ 向量数据库   │ 对象存储            │
│ PostgreSQL  │ ChromaDB    │ MinIO/S3           │
│ SQLite      │ FAISS       │ 本地文件系统         │
└─────────────┴─────────────┴─────────────────────┘
```

### 3.2 各层职责
1. **结构化数据库**（PostgreSQL/SQLite）
   - 存储文件元数据
   - 存储切片索引关系
   - 存储权限信息
   - 存储分类标签

2. **向量数据库**（ChromaDB/FAISS）
   - 存储文本内容的向量嵌入
   - 支持语义搜索
   - 支持相似度匹配
   - 支持AI学习

3. **对象存储**（MinIO/本地文件系统）
   - 存储原始文件
   - 存储处理后的中间文件
   - 存储图片等二进制内容

## 4. 文件处理流程

### 4.1 上传流程
```
用户上传 → 文件类型识别 → 权限验证 → 原始文件存储(对象存储)
       ↓
元数据提取 → 结构化数据库记录
       ↓
内容解析 → 智能切片 → 向量化 → 向量数据库存储
       ↓
分类标签 → 结构化数据库关联
```

### 4.2 智能切片算法
针对复杂内容元素，采用多级切片策略：

#### 4.2.1 一级切片（基于文档结构）
- **PDF**：按页面、章节、段落
- **Word**：按章节、标题、段落
- **Excel**：按工作表、表格区域
- **图片**：OCR识别后按语义段落

#### 4.2.2 二级切片（基于语义边界）
- 使用NLP模型识别语义边界
- 基于话题连贯性分割
- 基于实体连续性
- 最小切片单位：200-500字符

#### 4.2.3 三级切片（基于查询优化）
- 动态切片：根据查询意图调整
- 上下文扩展：切片时保留前后文
- 重叠切片：避免信息丢失

### 4.3 切片优化策略
1. **表格智能处理**
   - 识别表格结构
   - 按行/列语义分组
   - 生成结构化描述

2. **图片内容提取**
   - OCR文字识别
   - 图像内容描述
   - 混合内容处理

3. **混合格式处理**
   - 保持格式关联性
   - 建立内容链接
   - 维护上下文关系

## 5. 数据库设计

### 5.1 结构化数据库表设计

#### 表1：knowledge_files（知识文件表）
```sql
CREATE TABLE knowledge_files (
    id UUID PRIMARY KEY,
    filename VARCHAR(500) NOT NULL,
    file_type VARCHAR(50),  -- pdf, docx, xlsx, image, feishu
    file_size BIGINT,
    upload_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    upload_user VARCHAR(100),
    category VARCHAR(50),   -- project_info, project_management, jira_spec
    status VARCHAR(20),     -- uploaded, processing, processed, error
    storage_path VARCHAR(1000),  -- 对象存储路径
    feishu_doc_id VARCHAR(200),  -- 飞书文档ID
    permissions JSONB,           -- 权限信息
    metadata JSONB              -- 扩展元数据
);
```

#### 表2：content_chunks（内容切片表）
```sql
CREATE TABLE content_chunks (
    id UUID PRIMARY KEY,
    file_id UUID REFERENCES knowledge_files(id),
    chunk_index INTEGER,          -- 切片序号
    chunk_type VARCHAR(50),       -- text, table, image, mixed
    content_text TEXT,            -- 文本内容
    content_summary TEXT,         -- 内容摘要
    semantic_context TEXT,        -- 语义上下文
    start_position INTEGER,       -- 在原文件中的起始位置
    end_position INTEGER,         -- 结束位置
    vector_id VARCHAR(200),       -- 向量数据库中的ID
    parent_chunk_id UUID,         -- 父切片ID（用于层级结构）
    metadata JSONB               -- 切片元数据（如表格结构、图片描述等）
);
```

#### 表3：knowledge_categories（知识分类表）
```sql
CREATE TABLE knowledge_categories (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    category_type VARCHAR(50),  -- project_info, project_management, jira_spec
    parent_id INTEGER REFERENCES knowledge_categories(id),
    created_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 表4：ai_learning_logs（AI学习日志表）
```sql
CREATE TABLE ai_learning_logs (
    id UUID PRIMARY KEY,
    chunk_id UUID REFERENCES content_chunks(id),
    query_text TEXT,              -- 用户查询
    used_context TEXT,            -- 使用的上下文
    response_text TEXT,           -- AI响应
    feedback_score INTEGER,       -- 反馈评分
    learning_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    agent_name VARCHAR(100)       -- 使用的AI代理
);
```

### 5.2 向量数据库设计
使用ChromaDB存储向量，包含以下集合：
- `knowledge_embeddings`：知识切片向量
- `document_embeddings`：文档级向量
- `category_embeddings`：分类向量

### 5.3 对象存储设计
目录结构：
```
uploads/
  ├── raw/           # 原始文件
  │   ├── project_info/
  │   ├── project_management/
  │   └── jira_spec/
  ├── processed/     # 处理后的文件
  └── thumbnails/    # 缩略图
```

## 6. 权限管理设计

### 6.1 飞书云文档权限处理
1. **权限同步机制**
   - 定期同步飞书文档权限
   - 建立用户-文档权限映射
   - 缓存权限信息

2. **访问控制**
   - 基于用户角色控制访问
   - 实时验证飞书权限
   - 优雅降级策略

3. **离线缓存**
   - 缓存已授权内容
   - 定期更新缓存
   - 缓存失效机制

### 6.2 通用权限模型
- **读取权限**：查看文件内容
- **写入权限**：上传/更新文件
- **管理权限**：分类管理、权限分配
- **审核权限**：内容审核、质量控制

## 7. API接口设计

### 7.1 文件上传接口
```http
POST /api/knowledge/upload
Content-Type: multipart/form-data

参数：
- file: 文件
- category: 分类（project_info/project_management/jira_spec）
- tags: 标签列表
- feishu_url: 飞书文档URL（可选）
```

### 7.2 知识检索接口
```http
GET /api/knowledge/search
参数：
- query: 搜索关键词
- category: 分类过滤
- file_type: 文件类型过滤
- semantic_search: true/false（是否启用语义搜索）
- limit: 返回数量
```

### 7.3 AI学习接口
```http
POST /api/knowledge/ai-learn
参数：
- query: 用户查询
- context_size: 上下文大小
- use_knowledge: 是否使用知识库
- agent_type: 使用的AI代理类型
```

### 7.4 文件管理接口
- `GET /api/knowledge/files` - 获取文件列表
- `GET /api/knowledge/files/{id}` - 获取文件详情
- `DELETE /api/knowledge/files/{id}` - 删除文件
- `PUT /api/knowledge/files/{id}/category` - 更新分类

## 8. 技术栈选择

### 8.1 后端技术
- **Web框架**：Flask（与现有系统保持一致）
- **数据库**：SQLite（开发）/ PostgreSQL（生产）
- **向量数据库**：ChromaDB（轻量级，易集成）
- **对象存储**：本地文件系统 + MinIO（可选）
- **文件解析**：
  - `python-docx`：Word文档
  - `pandas`：Excel/CSV
  - `PyPDF2` / `pdfplumber`：PDF
  - `Pillow`：图片处理
  - `pytesseract`：OCR
- **NLP处理**：
  - `sentence-transformers`：文本向量化
  - `spaCy` / `jieba`：中文分词
  - `transformers`：语义分析

### 8.2 前端技术
- **现有前端扩展**：在现有HTML/JS基础上增加文件上传组件
- **组件库**：使用现有样式，保持一致性
- **文件预览**：集成PDF.js、SheetJS等

### 8.3 部署考虑
- **增量部署**：不影响现有Jira风险分析功能
- **数据迁移**：逐步迁移，支持回滚
- **性能优化**：异步处理大文件，分批处理

## 9. 实施计划

### 阶段1：基础架构搭建（1-2周）
1. 数据库设计和创建
2. 文件上传基础API
3. 对象存储集成
4. 基础前端组件

### 阶段2：文件解析引擎（2-3周）
1. 各种文件格式解析
2. 智能切片算法开发
3. 向量数据库集成
4. 权限管理系统

### 阶段3：AI集成优化（1-2周）
1. 知识检索接口
2. AI学习接口
3. 语义搜索优化
4. 反馈学习机制

### 阶段4：测试和优化（1周）
1. 性能测试
2. 权限测试
3. 用户体验优化
4. 文档编写

## 10. 风险与应对

### 10.1 技术风险
- **复杂文件解析**：采用成熟库+自定义处理
- **切片精度问题**：多级切片+反馈优化
- **性能问题**：异步处理+缓存策略

### 10.2 业务风险
- **权限限制**：飞书API限制，需申请权限
- **数据安全**：敏感内容处理，需加密存储
- **用户接受度**：逐步推广，收集反馈

### 10.3 应对策略
- **逐步迭代**：小步快跑，快速验证
- **监控报警**：关键流程监控
- **备份恢复**：定期备份，支持回滚

## 11. 成功标准
1. 支持所有目标文件格式的上传和解析
2. 智能切片准确率>85%
3. 知识检索响应时间<2秒
4. AI回答质量提升>30%
5. 用户满意度>4.5/5.0

---
*设计文档版本：v1.0*
*设计时间：2026-04-21*
*设计目标：为Jira风险分析平台构建智能知识库系统*